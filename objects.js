//In JavaScript, we need objects whenever we want to store, organize, and work with related data and behavior together.

let cousre = {
    title : "javascript",
    description : "core concepts of javascript",
    rating:5
};

console.log(cousre);
console.log(typeof(cousre));

// If i want to assess a single property of object then we can use dot notation

console.log(cousre.title);

// we can also use bracket notation to access the property of object
console.log(cousre["title"]);